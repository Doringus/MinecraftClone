#pragma once

#include <thread>
#include <vector>

namespace utils {

	/*
	class ThreadPool {
	public:
		ThreadPool(size_t threadCount = 4);
		~ThreadPool() noexcept;

	private:
		bool m_IsDone;
		std::vector<std::thread> m_Threads;
	};
	*/
}